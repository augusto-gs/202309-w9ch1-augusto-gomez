import { styled } from "styled-components";

const FilmsPageStyled = styled.section`
  .list-title {
    text-align: center;
    font-size: 50px;
  }

  .movies-list {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
`;

export default FilmsPageStyled;
